name: audio_meta
description: pure dart, zero dependency, cross-platform audio metadata extraction library
version: 1.0.1
repo: https://github.com/Milchkonsument/audio_meta
repository: https://github.com/Milchkonsument/audio_meta

environment:
  sdk: ^3.0.0

dependencies:

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
